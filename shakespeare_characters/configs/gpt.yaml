device: "cpu"
model:
  arch: "GPTModel"
  context_length: 8
  d_model: 96
  num_heads: 6
  n_layers: 3
train:
  batch_size: 128
  eval_interval: 100
  eval_iters: 1
  learning_rate: 3.e-4
  max_iters: 20_000
  # train_set_length: 9
wandb:
  name: "gpt"
